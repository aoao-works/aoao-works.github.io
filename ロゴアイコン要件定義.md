# aoao works シンボルマーク 要件定義書

| 項目 | 内容 |
|---|---|
| 対象 | aoao works シンボルマーク（アイコン単体） |
| バージョン | v1.0 |
| 最終更新 | 2026-09-10 |
| 位置づけ | この文書だけでマークを完全に再現できることを目的とする。外部資料への依存はない。 |

---

## 1. 概要

aoao works のシンボルマークは、**角丸の正方形モジュールを 10 個並べた単色の幾何学マーク**である。
モチーフは **テンキー（10 キー / 数字キーパッド）**。3×3 に並ぶ 9 個が数字キー（1〜9）、
その下・中央に置かれた 1 個が **0 キー**を表す。

- 図形は単色。色・グラデーション・立体表現に依存しない。
- すべての寸法は基準値 **m**（モジュール1辺）からの比率で定義し、絶対値（px 等）で固定しない。
- favicon（16px）でも各モジュールが分離して見えることを要件とする。

---

## 2. コンセプト

| 要素 | 意味 |
|---|---|
| 3×3 のモジュール | テンキーの数字キー 1〜9 |
| 下・中央のモジュール | テンキーの **0 キー** |
| モジュールの集合 | 「works」＝ 最小単位を組み立てて全体をつくる |

> 数字キーの配列（電卓式に 7・8・9 が上か、電話式に 1・2・3 が上か）は問わない。
> マークは数字を描かず、キーの並びのシルエットのみを表す。
> **下・中央のモジュールは必ず中央列（列2）に置く。** これは 0 キーの位置であり、装飾ではない。

---

## 3. 座標系と基本パラメータ

### 3.1 記号

| 記号 | 定義 |
|---|---|
| **m** | モジュールの1辺の長さ（唯一の自由パラメータ。すべてはここから決まる） |
| **g** | モジュール間の余白（ギャップ） |
| **r** | モジュールの角丸半径 |
| **p** | ピッチ（隣り合うモジュールの左辺どうしの間隔）＝ m + g |

### 3.2 値（標準）

| パラメータ | 式 | m = 6 のとき | m = 60 のとき |
|---|---|---|---|
| g | m ÷ 6 | 1 | 10 |
| r | m ÷ 6 | 1 | 10 |
| p | m + g = 7m ÷ 6 | 7 | 70 |
| 全体の幅 W | 2p + m = 10m ÷ 3 | 20 | 200 |
| 全体の高さ H | 3p + m = 9m ÷ 2 | 27 | 270 |
| 縦横比 W : H | 20 : 27 | ≈ 1 : 1.35 | ≈ 1 : 1.35 |

- **g と r は必ず同じ値（m ÷ 6）にする。**
- 角丸はすべてのモジュールの4隅に等しく適用する。

---

## 4. モジュール配置

座標は各モジュールの**左上角**。原点は左上、x は右、y は下。列の左辺 x = 0, p, 2p / 行の上辺 y = 0, p, 2p, 3p。

| # | キー | 列 | 行 | 左上 (x, y) ＝ m=6 | 左上 (x, y) ＝ m=60 |
|---|---|---|---|---|---|
| 1 | 数字 | 1（左） | 1 | (0, 0) | (0, 0) |
| 2 | 数字 | 2（中） | 1 | (7, 0) | (70, 0) |
| 3 | 数字 | 3（右） | 1 | (14, 0) | (140, 0) |
| 4 | 数字 | 1 | 2 | (0, 7) | (0, 70) |
| 5 | 数字 | 2 | 2 | (7, 7) | (70, 70) |
| 6 | 数字 | 3 | 2 | (14, 7) | (140, 70) |
| 7 | 数字 | 1 | 3 | (0, 14) | (0, 140) |
| 8 | 数字 | 2 | 3 | (7, 14) | (70, 140) |
| 9 | 数字 | 3 | 3 | (14, 14) | (140, 140) |
| 10 | **0** | 2（中） | 4 | **(7, 21)** | **(70, 210)** |

```
 ┌──┐┌──┐┌──┐        1〜9 : 数字キー（3×3）
 │  ││  ││  │
 └──┘└──┘└──┘
 ┌──┐┌──┐┌──┐
 │  ││  ││  │
 └──┘└──┘└──┘
 ┌──┐┌──┐┌──┐
 │  ││  ││  │
 └──┘└──┘└──┘
      ┌──┐              0 : 中央列・4行目
      │  │
      └──┘
```

---

## 5. 作図手順（ゼロから）

1. モジュール1辺 **m** を決める。
2. **g = r = m ÷ 6**、**p = m + g** を計算する。
3. 一辺 m の正方形を、`x = 列 × p`（列 = 0,1,2）、`y = 行 × p`（行 = 0,1,2）に **9 個**置く。
4. 10 個目の正方形を `x = p`、`y = 3p` に置く（中央列・4 行目 ＝ 0 キー）。
5. 10 個すべての角を半径 **r** で丸める。
6. 10 個すべてを**同一色**で塗る。線・影・効果は付けない。
7. マークのバウンディングボックスは **幅 10m/3 × 高さ 9m/2**。書き出し時はこの外接矩形で切り出す。

---

## 6. 標準 SVG（そのまま利用可）

`viewBox="0 0 20 27"`（m = 6）。色は用途に応じて `fill` を差し替える。

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 27" fill="#004466" role="img" aria-label="aoao works">
  <rect x="0"  y="0"  width="6" height="6" rx="1"/>
  <rect x="7"  y="0"  width="6" height="6" rx="1"/>
  <rect x="14" y="0"  width="6" height="6" rx="1"/>
  <rect x="0"  y="7"  width="6" height="6" rx="1"/>
  <rect x="7"  y="7"  width="6" height="6" rx="1"/>
  <rect x="14" y="7"  width="6" height="6" rx="1"/>
  <rect x="0"  y="14" width="6" height="6" rx="1"/>
  <rect x="7"  y="14" width="6" height="6" rx="1"/>
  <rect x="14" y="14" width="6" height="6" rx="1"/>
  <rect x="7"  y="21" width="6" height="6" rx="1"/>
</svg>
```

- 大きい座標系が必要なら全数値を 10 倍する（`viewBox="0 0 200 270"`、`width="60"`、`rx="10"`）。
- CSS から色を制御する場合は `fill="currentColor"` にする。

---

## 7. カラー

マークは**常に単色**。次の 3 バリエーションのみを用いる。

| バリエーション | 色 | 用途 |
|---|---|---|
| 通常 | Navy `#004466` | 白・淡色背景 |
| 反転 | White `#FFFFFF` | ネイビー・濃色・写真背景 |
| 1色（黒） | `#000000` | 単色印刷・箔押し・スタンプ・FAX |

- 背景とのコントラスト比は **4.5:1 以上**を確保する。
- グラデーション、2 色以上の塗り分け、差し色（Amber `#E2A33B` を含む）は不可。

---

## 8. サイズ規定

### 8.1 最小サイズ

| 媒体 | 最小サイズ（マーク幅） |
|---|---|
| デジタル | **32px** |
| 印刷 | **10mm** |

これ以下に縮小しない。

### 8.2 小サイズ版（描画幅 32px 以下で使用）

小サイズでは標準の余白・角丸だと潰れるため、専用の値に切り替える。**モジュールの数・配置は変えない。**

| パラメータ | 標準 | 小サイズ版 |
|---|---|---|
| g | m ÷ 6 | **m ÷ 3** |
| r | m ÷ 6 | **m ÷ 10** |
| W（= 2p + m） | 10m/3 | 11m/3 |
| H（= 3p + m） | 9m/2 | 5m |

小サイズ版 SVG（`viewBox="0 0 22 30"`、m = 6・g = 2・r = 0.6）:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 22 30" fill="#004466">
  <rect x="0"  y="0"  width="6" height="6" rx="0.6"/>
  <rect x="8"  y="0"  width="6" height="6" rx="0.6"/>
  <rect x="16" y="0"  width="6" height="6" rx="0.6"/>
  <rect x="0"  y="8"  width="6" height="6" rx="0.6"/>
  <rect x="8"  y="8"  width="6" height="6" rx="0.6"/>
  <rect x="16" y="8"  width="6" height="6" rx="0.6"/>
  <rect x="0"  y="16" width="6" height="6" rx="0.6"/>
  <rect x="8"  y="16" width="6" height="6" rx="0.6"/>
  <rect x="16" y="16" width="6" height="6" rx="0.6"/>
  <rect x="8"  y="24" width="6" height="6" rx="0.6"/>
</svg>
```

---

## 9. クリアスペース

マークの外接矩形の周囲に、最小限の余白を確保する。

| 方向 | 最小余白 |
|---|---|
| 左右 | **1 モジュール（1m）** |
| 上下 | **1.5 モジュール（1.5m）** |

- この内側にテキスト・図形・他のロゴを配置しない。
- 結果として保護領域は `幅 (10m/3 + 2m) × 高さ (9m/2 + 3m)` ＝ `16m/3 × 15m/2`、
  比率 **32 : 45 ≒ 1 : 1.41（白銀比 / 1:√2）** の長方形になる。

---

## 10. プロポーション（設計の根拠）

- マーク本体の縦横比は **W : H = 20 : 27 ≒ 1 : 1.35**。これは m ÷ 6 の余白から一意に決まる値で、調整しない。
- **黄金比（1:1.618）は採用しない。** 3列4行の点構成は余白を広げても外接比が最大でも約 1:1.5 で、構造上 1.618 に到達しない。
- **白銀比（1:√2）はクリアスペースとアプリアイコンのセーフエリアに用いる**（§9・§11.2）。マーク自体の形は変えない。

---

## 11. 配置ルール

### 11.1 禁止事項（Don't）

1. 指定色以外で着色する / 複数色にする / グラデーションをかける
2. 縦横比を変える（拡大・縮小は等比のみ）
3. 回転・傾斜させる
4. 影・光彩・立体・テクスチャなどの効果を加える
5. モジュールを分解・再配置する / 個数を増減する
6. モジュール間の余白 g を規定値から変える
7. 角丸 r を規定の比率から変える
8. 下・中央のモジュールを別の位置（左・右・列外）へ動かす

### 11.2 アプリアイコン

- 正方形アイコンの中央にマークを置く。
- **マークの高さ = アイコン1辺 ÷ √2（≒ 70%）** とし、上下左右の余白を均等にする。
- 背景は Navy `#004466`、マークは White。またはその反転。

### 11.3 ロックアップ（マーク＋「aoao works」）

- マークの右に文字を置く場合、間隔は **1 モジュール（1m）**。
- マークの高さを文字のキャップハイトにそろえる。
- 文字は Outfit（Bold）。和文併記時は Noto Sans JP。

---

## 12. 書き出しアセット一覧

| アセット | 形式 | サイズ / 備考 |
|---|---|---|
| マスター | SVG | `viewBox="0 0 20 27"`、`fill="currentColor"` |
| 小サイズ版 | SVG | `viewBox="0 0 22 30"` |
| ラスター（通常・透過） | PNG | 32 / 48 / 64 / 128 / 256 / 512 / 1024 px |
| ラスター（反転・透過） | PNG | 同上 |
| favicon | ICO + PNG | 16 / 32 / 48 px（16・32 は小サイズ版） |
| アプリアイコン | PNG | 1024px、§11.2 のセーフエリア |
| 単色（黒） | SVG / PNG | 箔押し・1色印刷用 |

命名例: `aoao-mark_navy.svg` / `aoao-mark_reverse.svg` / `aoao-mark_black.svg` / `aoao-mark_sm.svg` / `aoao-favicon-32.png`

---

## 13. 受け入れ基準（チェックリスト）

- [ ] モジュールが 10 個で、配置が §4 の座標表と一致する
- [ ] 10 個目が中央列・4 行目（0 キーの位置）にある
- [ ] g と r が等しく、標準は m ÷ 6 / 小サイズ版は m ÷ 3・m ÷ 10 になっている
- [ ] 寸法が px 直値で固定されておらず、m に比例して変化する
- [ ] 単色（§7 の 3 色のいずれか）で、線・影・効果がない
- [ ] 外接比が 20 : 27（小サイズ版は 11 : 15）
- [ ] 16px で各モジュールが分離して見える
- [ ] クリアスペース（左右 1m・上下 1.5m）を確保できる余地がある
- [ ] §11.1 の禁止事項に該当しない

---

## 14. 変更履歴

| バージョン | 日付 | 内容 |
|---|---|---|
| v1.0 | 2026-09-10 | 初版。テンキーをモチーフとした 10 モジュール構成、m ÷ 6 の寸法規則、小サイズ版、クリアスペース、カラー、書き出し要件を確定。 |
